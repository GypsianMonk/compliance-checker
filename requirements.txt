langgraph>=0.2.0
langchain-core>=0.3.0
chromadb>=0.5.0
pydantic>=2.0.0

# Optional: only needed if you set OPENAI_API_KEY to use a real LLM
# instead of the built-in offline reasoning fallback.
# openai>=1.0.0

# Optional: only needed for native LangSmith tracing
# (set LANGCHAIN_TRACING_V2=true and LANGCHAIN_API_KEY as well).
# langsmith>=0.1.0
